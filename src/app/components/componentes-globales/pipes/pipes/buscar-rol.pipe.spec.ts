import { BuscarRolPipe } from './buscar-rol.pipe';

describe('BuscarRolPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarRolPipe();
    expect(pipe).toBeTruthy();
  });
});
