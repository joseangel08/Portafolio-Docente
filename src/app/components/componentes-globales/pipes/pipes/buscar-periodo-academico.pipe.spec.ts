import { BuscarPeriodoAcademicoPipe } from './buscar-periodo-academico.pipe';

describe('BuscarPeriodoAcademicoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPeriodoAcademicoPipe();
    expect(pipe).toBeTruthy();
  });
});
