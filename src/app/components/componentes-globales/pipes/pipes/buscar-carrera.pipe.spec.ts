import { BuscarCarreraPipe } from './buscar-carrera.pipe';

describe('BuscarCarreraPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarCarreraPipe();
    expect(pipe).toBeTruthy();
  });
});
